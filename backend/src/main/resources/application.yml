spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/minerva-dev
    username: minerva-dev
    password: dev
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect